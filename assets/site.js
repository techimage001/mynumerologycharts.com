
(()=>{const d=document;const root=d.documentElement;const saved=localStorage.getItem('mnc-theme');if(saved)root.dataset.theme=saved;const menu=d.querySelector('[data-menu]');const nav=d.querySelector('.nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});d.querySelector('[data-theme]')?.addEventListener('click',()=>{const next=root.dataset.theme==='dark'?'light':'dark';root.dataset.theme=next;localStorage.setItem('mnc-theme',next)});
const key='mnc-cookie-consent';const banner=d.querySelector('.cookie-banner');const modal=d.querySelector('.cookie-modal');const stored=localStorage.getItem(key);if(!stored)banner?.removeAttribute('hidden');function save(v){localStorage.setItem(key,JSON.stringify(v));banner?.setAttribute('hidden','');modal?.setAttribute('hidden','');window.dispatchEvent(new CustomEvent('mnc-consent',{detail:v}))}d.querySelectorAll('[data-consent="accept"]').forEach(b=>b.addEventListener('click',()=>save({necessary:true,analytics:true,at:new Date().toISOString()})));d.querySelectorAll('[data-consent="reject"]').forEach(b=>b.addEventListener('click',()=>save({necessary:true,analytics:false,at:new Date().toISOString()})));d.querySelectorAll('[data-consent="manage"],[data-cookie-settings]').forEach(b=>b.addEventListener('click',()=>modal?.removeAttribute('hidden')));d.querySelector('[data-consent="save"]')?.addEventListener('click',()=>save({necessary:true,analytics:!!d.querySelector('#analyticsConsent')?.checked,at:new Date().toISOString()}));d.querySelector('[data-consent="close"]')?.addEventListener('click',()=>modal?.setAttribute('hidden',''));
const signupModal=d.querySelector('#signupModal');
async function prepareSignup(form){const started=form.querySelector('[name="started"]');const token=form.querySelector('[name="js_token"]');const status=form.querySelector('[data-signup-status]');if(started)started.value=String(Date.now());if(token)token.value='';try{const r=await fetch('api/form-token.php',{credentials:'same-origin',headers:{'Accept':'application/json'}});if(!r.ok)throw new Error('token');const data=await r.json();if(token)token.value=data.token||'';if(status)status.textContent='';}catch{if(status)status.textContent='The secure form could not be prepared. Please refresh and try again.';}}
function openSignup(){signupModal?.removeAttribute('hidden');const form=signupModal?.querySelector('.signup-form');if(form)prepareSignup(form);setTimeout(()=>signupModal?.querySelector('input[type="email"]')?.focus(),0)}
function closeSignup(){signupModal?.setAttribute('hidden','')}
d.querySelectorAll('[data-open-signup]').forEach(b=>b.addEventListener('click',openSignup));d.querySelectorAll('[data-close-signup]').forEach(b=>b.addEventListener('click',closeSignup));signupModal?.addEventListener('click',e=>{if(e.target===signupModal)closeSignup()});d.addEventListener('keydown',e=>{if(e.key==='Escape'&&!signupModal?.hasAttribute('hidden'))closeSignup()});d.querySelectorAll('.signup-form').forEach(f=>prepareSignup(f));
const pages=window.MNC_PAGES||[];function card(p){return `<article><h2><a href="${p.slug}.html">${p.h1}</a></h2><p>${p.answer}</p><a class="btn btn-secondary" href="${p.slug}.html">Open page</a></article>`}const cd=d.querySelector('#calculator-directory');if(cd)cd.innerHTML=pages.filter(p=>p.group==='Calculators').map(card).join('');const ld=d.querySelector('#learning-directory');if(ld)ld.innerHTML=pages.filter(p=>p.group==='Learn').map(card).join('');const az=d.querySelector('#az-directory');if(az)az.innerHTML=pages.filter(p=>p.index!==false).sort((a,b)=>a.h1.localeCompare(b.h1)).map(p=>`<a href="${p.slug}.html">${p.h1}</a>`).join('');
})();
